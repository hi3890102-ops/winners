name: Manager home summary hotfix test

on:
  push:
    branches:
      - hotfix-manager-home-summary
    paths:
      - '.github/workflows/run-manager-home-summary-hotfix-test.yml'
      - 'security/manager-home-summary-hotfix.test.mjs'
      - 'index.html'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hotfix-manager-home-summary
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test security/manager-home-summary-hotfix.test.mjs
